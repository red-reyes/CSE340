<div class="account-container">
    <% if (typeof flashMessage !== 'undefined') { %>
        <div class="flash-message">
            <%= flashMessage %>
        </div>
    <% } %>
    
    <h1><%= title %></h1>

    <form id="add-classification-form" action="/inventory/add-classification" method="POST">
        <div class="form-group">
            <label for="classification_name">Classification Name:</label>
            <input 
                type="text" 
                id="classification_name" 
                name="classification_name" 
                required 
                pattern="^[a-zA-Z0-9 ]+$" 
                title="Classification name can only contain letters, numbers, and spaces."
                placeholder="Enter classification (e.g., Sports Car)"
            >
        </div>
        
        <button type="submit">Add Classification</button>
    </form>
</div>
<script>
    document.getElementById('add-classification-form').addEventListener('submit', function(event) {
        var classificationName = document.getElementById('classification_name').value;
        var pattern = /^[a-zA-Z0-9 ]+$/;

        if (!pattern.test(classificationName)) {
            event.preventDefault();
            alert('Classification name can only contain letters, numbers, and spaces.');
        }
    });
</script>
